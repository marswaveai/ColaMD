# ColaMD Design

ColaMD 的界面判断依据。写界面之前先读它；新需求与它冲突时，先改它，再写代码。

> Writing should be this simple.

## 这份文档记什么

- 只记**原则**：在不知道具体数值的情况下，也能用它判断一个方案合不合适。
- **数值与具体行为以代码为准**：尺寸、间距、颜色、圆角、时长在 `src/renderer/themes/` 与各组件里，菜单项和快捷键在各自的定义处。文档不复述它们——改一个像素不需要改这里。
- **被否掉的方案和过程归 PR 与 git 历史**，只把结论提炼成原则；事故现场不写进文档。

## 1. 设计原则

### 如非必要，勿增实体

ColaMD 是轻量 Markdown 编辑器，不追求功能堆叠。每增加一个按钮、图标、提示或面板，先回答：它解决什么明确问题？已有的 Markdown 语法、快捷键或菜单能否完成？它会不会变成持续的视觉噪音？默认不增加。

### 内容优先

- 编辑器内容是界面中心，工具控件不得抢夺注意力：默认低调，只在 hover、focus 或 active 时给反馈。
- 编辑器保持留白，不为了填满窗口而扩大内容宽度；正文区下方的空白也要能放光标——它是一块画布，不是一张纸的边界。
- 不使用常驻工具栏和状态栏，不用装饰性 UI 模拟功能。
- 会丢掉用户内容或接管用户文件的动作，必须给出明确确认。
- 外部 Agent 修改文件时，复用已有的状态点和刷新机制，不新增常驻提示条。
- **「已编辑」必须有基线**：程序化载入内容（打开文件、恢复会话、载入示例）都不算用户编辑，文档要保持干净；只有用户真正改了内容才提示、才保存（提示就是标签上那个小圆点）。新建窗口必须从干净状态开始，一个字没动就关窗不该问要不要保存。
- **状态用浓度表达，不用盒子**：悬停或开启时不要出现一块又消失的色块——色块一出现就把眼睛从文档里拽出去。同一个字形颜色分几档浓度（静置最低、悬停次之、开启最高、禁用更低）就能说完所有状态。
- 弱化控件就弱化它的**字形颜色**，不要用元素级透明度：透明度会连它里面的提示框一起变淡。

### 一致性优先

同一类问题只允许一种主要解决方式；同类控件共享尺寸、间距、颜色、圆角、动画和图标语言，不为单个场景发明例外。

- **同一句话，不是相似的句子**：活动标签、侧栏的选中项、文件列表的当前行是同一种画法（纸面 + 细线 + 同一个圆角）。新控件先去已有配方里找，找不到再谈新配方。标签占满整行后不再需要圆角（纸柱自己就是形状），这条配方用于剩下的两侧栏控件。

### 颜色属于层级，不属于装饰

外壳是外壳色，文档是纸色，一个窗格只能属于其中一层。把纸色的面板一路顶到窗口顶边，它和外壳就会在同一行里相遇，那一行裂出一条灰/纸缝——眼睛读成破损，而不是「两种表面」。要么让这一列属于外壳，要么让它留在外壳下面。

- 颜色一律从主题变量派生，不写死：写死的灰在深色主题里不可见，写死的强调色在深色主题里刺眼。
- 控件默认背景透明，悬停用低透明度的中性色，不制造新的色块。
- 强调色只属于正文（链接），不进外壳：选中是「位置」，不是「重要」。
- 弱化色只给次要文字和次要图标，且必须保持能读的对比度，不许借给需要阅读的文字。
- 深色和浅色主题各过一遍，才算改完 UI。

### 形状与尺寸

- **同心**：外层圆角要比内层大出内边距；两者取同一个值时，里面的小块看着是「悬」在角上。
- **等高不等于等量**：同样的高度下，更宽的控件更重。一方是窄标签、一方是宽药丸时，协调的关系是「外框等高、内容低一档」，不是所有矩形都用同一个高度。
- **相邻边缘要对到线上**：同一条竖边上相邻的两个元素以那条线为基准对齐，差一个像素也看得出来；边界线算在外层尺寸之内。
- 动效只用于表达状态，不用于装饰。

### 文字与提示

- **截断的名字由浮层说出全名，不要滚动**：名字在自己那一行里左右滑动，会从图标下滑过去、盖住它所属的行。浮层放在一侧、与该行齐平，不遮住它正在描述的内容，只在真正被截断时才出现。
- **悬浮提示不得淡入**：要么完全不透明，要么不存在；透明度过渡会被拍到半透明，看上去像文字穿透提示框。
- 提示不能遮挡主要内容，也不能挡住鼠标；盖住文字的按钮不得半透明。
- 提示不要抢在动作之前：用户停在一个控件上通常是要点它，不是要读说明书。
- 不用系统的 `title` 浮层代替我们自己的提示——它又慢，又不像我们自己的东西。

## 2. 结构性约定

以下不是数值，是「这个东西是什么」。动手之前先确认没有把它的身份换掉。

### 窗口与顶行

- 一个窗口顶部永远只有一行：标签栏与标题栏共用同一行，标签条是标题栏里的弹性成员，不是压在上面的浮层。（曾做成浮层，线、色、按钮三层症状全部源于这个分裂。）
- 顶行只有一套背景、一条基线、一套层级；活动标签是**一整列纸色**，从窗口上沿一直落到行底，把基线切开、开口通向正文。标签占满整行高度，不用圆角、不靠间隙分隔：纸色和外壳色的对比本身就是选中态，分隔只由一条**接通上下**的细线承担（行首那条也算，不许浮在中间）。空的那一段仍然可以拖拽窗口。
- **左端只属于文档**：红绿灯之后直接就是标签条，中间不放别的东西（Chrome 的那种感觉）。左边越简单越好 —— 曾把窗口自己的三个按钮搬到左端，观感不如右侧。
- **右端是一列**：行里是保存状态和三个按钮（文档统计、源码模式、文件面板），行下面就是文件面板的领地，按钮和它管的列表待在同一侧。
- **行里的图标只有一个灰**：加号、文档统计、源码模式、文件面板共用同一个浓度，谁都不靠变深变浅表达状态（用 `--row-icon-color` 这一处定义，别再各写各的）。
- **外壳的线是一个颜色，而且是不透明的**：标签之间、行基线、活动标签两侧、面板边缘都用 `--shell-line` 一处定义。不要用半透明的线：它会被底下的颜色改写，同一个值在两种底上就变成两条深浅不同的线（面板边缘在行里偏深、在行下偏浅，看着像两条线接在一起）。
- 面板在右端，它的左边缘就是正文区的右边界：面板越宽，正文看得越少，但正文列在剩下的空间里重新居中，不跟着面板往左挤。
- 标题永远居中：标题栏中部的辅助元素（悬停出现的按钮、保存状态）不参与标题的居中计算，标题不许被挤偏。
- 标题栏是窗口拖拽区，其中的元素默认收不到鼠标事件：想点击必须显式声明自己可接收指针事件，标题栏内的浮层也必须绘制在正文之上。
- 标签栏始终存在：一个文档也是一个标签，顶部永远同一个形状。展示文件名由标签承担，标题栏不再承载文件名。
- 窗口的尺寸、位置与视图缩放要被记住；但保存的位置不在当前任何一块屏幕上时（比如外接屏拔了），退回默认尺寸，不能把窗口开到看不见的地方。

### 标签

- 标签显示**文档名**，不带 markdown 后缀：窄标签里每个标签都相同的那一截后缀是噪音，而且它落在截断点上会和省略号糊在一起。真实文件名由窗口标题和文件列表承担。
- 宽度跟着名字走，但要有下限和上限：短名字不能塌成一个小方块，长名字也不能无限长；空间不够时先压缩，压到下限才让这一条滚动。未保存状态只用标签上那个弱化的小圆点表示——顶行不再另给一个「已编辑」的字（面板、源码模式、未保存都是看得见的事，多一个常亮的提示就是多一分干扰）。
- 新建入口分散在 `⌘T`、File 菜单、文件列表（⌘ 加点击与右键第一项）和标签栏右端紧跟着最后一个标签的那个加号；标题栏不放加号——它会在标签栏出现时突然消失。
- 每个标签独立持有内容、未保存状态、撤销历史、滚动位置和显示模式，切换不许互相影响；实现上只用一个编辑器实例，切换就是换状态快照。
- 切换标签不得丢掉改了还没存的内容。只有活动标签监听文件变化，切回一个标签时校验它的磁盘版本。
- `⌘W` 关当前标签，关掉最后一个标签时关窗口。
- **标签页不是工作区**：不持久化，重启不恢复标签组；不做固定标签、标签分组、跨窗口拖拽、全量文件树。新窗口只能由用户主动要，标签不跨窗口移动。
- **打开文件不制造重复**：已经打开的就聚焦它所在的窗口并激活对应标签；否则用当前的空标签装下它，没有空标签才在当前窗口新开一个。多窗口不是打开文件的副作用。文件列表的点击是浏览不是打开——换目录看文件不该越点标签越多。

### 文件面板

- 只做轻量文件列表：不做持久化工作区，不做全量文件树。
- 打开文件时浏览它所在的目录；没有打开文件时浏览「文稿」，不可用则退回桌面；可进目录、可回上级。
- 拖拽把手轻量：不显示常驻把手图标，只有边缘热区加 hover 反馈。
- 「文件 / 大纲」切换保持平面极简，活动片与活动标签是同一句配方；面板留在外壳下面，不铺到窗口顶边。
- 文件条目用系统原生右键菜单，不自绘菜单，不加常驻按钮。

### 图标

- 功能图标统一用线性 SVG；禁止用 Unicode 字符、Emoji 或系统字体字符顶替（`↰`、`▸`、`▶` 这类）。
- 同一组图标共享 `viewBox`、尺寸、线宽、端点与连接方式；文件、文件夹、返回上级三者必须同线宽、同颜色、同尺寸、同垂直对齐。
- 图标不得用填充块制造与其他图标不一致的视觉重量，除非它本身在表达选中或状态。
- 每个图标按钮要可识别、有 `title`、有 `aria-label`、有 hover 说明，并具备它该有的 active 与 disabled 状态。说明用自然简洁的中文，不要直译，也不要只写英文 API 名。

### 菜单与快捷键

- 重要操作必须能通过菜单或快捷键完成，不能只依赖隐藏图标。
- 菜单文案全部走 `labels`（中英同步），不自带图标；只在文案会被 `label` 覆盖、且它不会自己画图标时才用系统 `role`。
- 导出统一放在 File 菜单里。

## 3. 已知的死路

试过并确认不行的方向，别再试一遍：

- 让标签条浮在标题栏之上——同一条顶行的分裂，症状会散到线、色和按钮三处。
- 用系统 `role` 生成带图标的菜单项——它会自己画图标、自己取英文名。
- 悬停才显示自定义滚动条滑块——Chromium 不会因 hover 重绘；macOS 直接用系统浮层滚动条，其余平台用细滚动条。
- 在标题栏放一个会「出现 / 消失」的加号——标签栏出现时它会突然不见。

## 4. 动手之前

- **把「感觉不对」翻译成可量的差异**：每一次「别扭」都能落到具体差异上——差一个像素、同一行里出现两个层级色、圆角不同心、两个控件等高但不等宽。先在真实窗口里量（computed style + 截图取色），再动手；一次只动一个变量，改完再量一遍确认方向。
- 改完 UI 要在**真窗口真机**里点一遍，深色和浅色各看一遍；浏览器复现页只能验证 CSS 机制，验不了 Electron 的运行时行为。
- 删 CSS 规则必须连规则体一起删，禁止把选择器并进下一条规则——这类错误类型检查查不出来。
- 主题派生变量必须定义在主题作用域里：定义在根作用域会被浅色默认值算死，之后不跟主题走。
- hover 才出现的控件：触发点必须自己可接收指针事件，命中区要覆盖从触发点到控件的整条路径，并且不要同时用禁用态当显隐门。
- 新增控件前先找有没有同职责的控件、样式或交互；不为单个控件复制一套 tooltip、按钮或图标样式。
- 编辑器逻辑、IPC 逻辑与视觉样式分离；共享视觉样式放在主题目录里。
- 动态创建图标用固定的 SVG 模板，不把用户输入拼进 SVG 或 HTML；新增主题变量时同步所有内置主题。

## 5. 贡献检查清单

提交界面改动前逐项确认：

- [ ] 这个元素确实必要，没有重复入口。
- [ ] 图标是线性 SVG，且与同类图标同语言、同尺寸、同线宽。
- [ ] 颜色来自主题变量，深色浅色都看过了。
- [ ] hover、active、disabled 状态完整且克制；状态用浓度表达，不用色块。
- [ ] 没有引入常驻工具栏、状态栏或额外面板。
- [ ] 菜单、快捷键与文件切换行为没有被破坏，标签之间不会互相影响。
- [ ] 在真机真窗口里点过一遍，类型检查与构建通过，`git diff --check` 干净。
- [ ] 规范不足时，先补本文档，再提交实现。
