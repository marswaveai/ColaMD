# ColaMD Design

ColaMD 的界面判断依据。写界面之前先读它；新需求与它冲突时，先改它，再写代码。

> Writing should be this simple.

## 这份文档记什么

- 只记**原则**：不知道具体数值，也能用它判断一个方案合不合适。
- **数值与具体行为以代码为准**：尺寸、间距、颜色、圆角、时长在 `src/renderer/themes/` 与各组件里，菜单项和快捷键在各自的定义处。文档不复述它们，改一个像素不需要改这里。
- 唯一允许出现细节的地方是「已知的坑」：试过并确认不行的方向，以及会**静默**出错的实现约束。它们是被烧出来的，不写下来就会再烧一次。
- 被否掉的方案与过程归 PR 和 git 历史，只把结论提炼成原则。

## 1. 设计原则

### 如非必要，勿增实体

ColaMD 是轻量 Markdown 编辑器，不追求功能堆叠。每增加一个按钮、图标、提示或面板，先回答：它解决什么明确问题？已有的 Markdown 语法、快捷键或菜单能否完成？它会不会变成持续的视觉噪音？默认不增加。

### 内容优先

- 编辑器内容是界面中心，工具控件不得抢夺注意力：默认低调，只在 hover、focus 或 active 时给反馈。
- 编辑器保持留白，不为了填满窗口而扩大内容宽度；正文区下方的空白也要能放光标——它是一块画布，不是一张纸的边界。
- 不使用常驻工具栏和状态栏，不用装饰性 UI 模拟功能。
- 会丢掉用户内容或接管用户文件的动作，必须给出明确确认。
- **文件里不属于正文的东西按字节写回**：属性区（YAML frontmatter）是用户文件的一部分，行尾符同理。它们不进编辑器、不参与排版，保存时原样拼回去，编辑器只重新排版正文。
- **属性区照原样显示，只压淡**：不折叠、不画成卡片、不换成一个「属性区（N 行）」的按钮。用凭空造出来的控件替掉用户文件里真实存在的内容，看着像设计，实际是拿界面骗人；里面的 `- `、`---`、`**` 是 YAML 字符，不参与 markdown 排版，也不参与分页。
- 外部 Agent 改文件时，复用已有的状态点和刷新机制，不新增常驻提示条。
- **「已编辑」必须有基线**：程序化载入（打开文件、恢复会话、载入示例）都不算用户编辑，文档要保持干净；只有用户真正改了内容才提示、才保存。
- **状态用浓度表达，不用盒子**：悬停或开启时不要出现一块又消失的色块——色块一出现就把眼睛从文档里拽出去。同一个字形颜色分几档浓度就能说完所有状态。
- 弱化控件就弱化它的**字形颜色**，不要用元素级透明度：透明度会连它里面的提示框一起变淡。

### 一致性优先

同一类问题只允许一种主要解决方式；同类控件共享尺寸、间距、颜色、圆角、动画和图标语言，不为单个场景发明例外。

- **同一句话，不是相似的句子**：活动标签、侧栏的选中项、文件列表的当前行是同一种画法。新控件先去已有配方里找，找不到再谈新配方。

### 颜色属于层级，不属于装饰

外壳是外壳色，文档是纸色，一个窗格只能属于其中一层。把纸色的面板一路顶到窗口顶边，它和外壳就会在同一行里相遇，那条缝被眼睛读成破损，而不是「两种表面」。

- 颜色一律从主题变量派生，不写死：写死的灰在深色主题里不可见，写死的强调色在深色主题里刺眼。
- **同一份文档里的横线只有一个粗细**：标题下划线、分隔线、引用块、表格线是同一类东西，谁都不比谁重要。
- 「界面语言」是一句承诺，管到**用户能看见的每一个字**：对话框的按钮和正文、保存和导出面板的标题与按钮名都要跟着走。
- 控件默认背景透明；强调色只属于正文（链接），不进外壳：选中是「位置」，不是「重要」。
- 弱化色只给次要文字和次要图标，且必须保持能读的对比度。
- 深色和浅色主题各过一遍，才算改完 UI。

### 形状与尺寸

- **同心**：外层圆角要比内层大出内边距；取同一个值时，里面的小块看着是「悬」在角上。
- **等高不等于等量**：同样的高度下，更宽的控件更重。窄标签与宽药丸之间协调的关系是「外框等高、内容低一档」，不是所有矩形都用同一个高度。
- **相邻边缘要对到线上**：同一条竖边上相邻的两个元素以那条线为基准对齐；边界线算在外层尺寸之内。
- **点击热区不小于 24×24**：可见字形可以更小，差额用一圈不可见的、不占布局的外圈补足。热区不许反过来改动布局，也不许为它放大可见尺寸。
- 动效只用于表达状态，不用于装饰。

### 文字与提示

- **截断的名字由浮层说出全名，不要滚动**：名字在自己那一行里左右滑动会从图标下滑过去、盖住它所属的行。浮层放在一侧、与该行齐平，不遮住它正在描述的内容，只在真正被截断时才出现。
- **悬浮提示不得淡入**：要么完全不透明，要么不存在。
- 提示不能遮挡主要内容，也不能挡住鼠标；盖住文字的按钮不得半透明。
- 提示不要抢在动作之前：用户停在一个控件上通常是要点它，不是要读说明书。
- 不用系统的 `title` 浮层代替我们自己的提示。

## 2. 结构性约定

以下不是数值，是「这个东西是什么」。动手之前先确认没有把它的身份换掉。

### 窗口与顶行

- 一个窗口顶部永远只有一行：标签栏与标题栏共用同一行，标签条是标题栏里的**弹性成员**，不是压在上面的浮层。
- 顶行只有一套背景、一条基线、一套层级；活动标签是**一整列纸色**，从窗口上沿一直落到行底，把基线切开、开口通向正文。标签占满整行高度，纸色与外壳色的对比本身就是选中态。
- **左端只属于文档**：macOS 是红绿灯之后直接就是标签条。**右端是一列**：保存状态和几个按钮（文档统计、源码模式、文件面板），行下面就是面板的领地。
- **红绿灯的圆心落在行的中线上**，参照的是原生工具栏的同一条规则。
- **行里的图标只有一个浓度**：加号、文档统计、源码模式、文件面板共用同一个灰，谁都不靠变深变浅表达状态。
- **外壳的线是一个颜色，而且是不透明的**：标签之间、行基线、活动标签两侧、面板边缘共用一处定义。半透明的线会被底下的颜色改写，同一个值在两种底上就变成两条深浅不同的线。
- 面板在右端时，它的左边缘就是正文区的右边界：面板越宽，正文看得越少，但正文列在剩下的空间里重新居中，不跟着面板往左挤。
- 面板的边缘线**不从行里穿上来**：行里那条竖线没有任何面变化支撑它，读起来是一道多余的划痕。
- 标题永远居中：标题栏中部的辅助元素（悬停出现的按钮、保存状态）不参与标题的居中计算。
- 标题栏是窗口拖拽区，其中的元素默认收不到鼠标事件：想点击必须显式声明自己可接收指针事件。
- 标签栏始终存在：一个文档也是一个标签。展示文件名由标签承担，标题栏不再承载文件名。
- 窗口的尺寸、位置与视图缩放要被记住；保存的位置不在任何一块屏幕上时（外接屏拔了），退回默认尺寸。
- **Windows 也只有一个顶行**：不画系统标题栏，也不在窗口里画菜单栏，系统的三个窗口按钮由系统画进这一行的右端，颜色由渲染层回报。菜单按钮在这一行的**最左端**，在标签条之前：Windows 用户在这边找菜单，右端留给系统的三个按钮。
- **键盘焦点框用主题墨色**：平台默认焦点环跟系统强调色走，那个颜色在主题里根本不存在。

### 标签

- 标签显示**文档名**，不带 markdown 后缀：窄标签里每个标签都相同的那一截后缀是噪音，落在截断点上还会和省略号糊在一起。真实文件名由窗口标题和文件列表承担。
- 宽度跟着名字走，但要有下限和上限；空间不够时先压缩，压到下限才让这一条滚动。未保存状态只用标签上那个弱化的小圆点表示。
- 每个标签独立持有内容、未保存状态、撤销历史、滚动位置和显示模式，切换不许互相影响；实现上只用一个编辑器实例，切换就是换状态快照。
- 切换标签不得丢掉改了还没存的内容。只有活动标签监听文件变化，切回一个标签时校验它的磁盘版本。
- **标签页不是工作区**：不持久化，重启不恢复标签组；不做固定标签、标签分组、跨窗口拖拽、全量文件树。新窗口只能由用户主动要。
- **打开文件不制造重复**：已经打开的就聚焦它所在的窗口并激活对应标签；否则用当前的空标签装下它，没有空标签才在当前窗口新开一个。多窗口不是打开文件的副作用。文件列表的点击是浏览不是打开。

### 文件面板

- 只做轻量文件列表：不做持久化工作区，不做全量文件树。
- 打开文件时浏览它所在的目录；没有打开文件时浏览「文稿」，不可用则退回桌面；可进目录、可回上级。
- 拖拽把手轻量：不显示常驻把手图标，只有边缘热区加 hover 反馈。
- 「文件 / 大纲」切换保持平面极简，活动片与活动标签是同一句配方；面板留在外壳下面，不铺到窗口顶边。
- **面板可以在左，也可以在右**，以**最小改动**为前提：只有面板、它的边缘线和它占的那一列换边，顶行的形状不变，标签条不缩进，行里那三个按钮也不搬。
- 文件条目用系统原生右键菜单，不自绘菜单，不加常驻按钮。
- 「返回上一级」是**列表里的一行，不是一种特殊行**：共用同一套圆角、悬停和字色，身份由文字说明，不靠破格的盒子。
- 同一排的图标要在**同一个坐标系**里画：把一个 16 单位的坐标系塞进 10px 的格子，整体会缩小、描边会变细，看起来就成了另一个控件。

### 图标

- 功能图标统一用线性 SVG；禁止用 Unicode 字符、Emoji 或系统字体字符顶替。
- 同一组图标共享 `viewBox`、尺寸、线宽、端点与连接方式；文件、文件夹、返回上级三者必须同线宽、同颜色、同尺寸、同垂直对齐。
- 图标不得用填充块制造与其他图标不一致的视觉重量，除非它本身在表达选中或状态。
- 每个图标按钮要可识别、有 `title`、有 `aria-label`、有 hover 说明，并具备它该有的 active 与 disabled 状态。说明用自然简洁的中文，不要直译，也不要只写英文 API 名。
- 图标要指向它管的东西所在的一侧。

### 菜单与快捷键

- 重要操作必须能通过菜单或快捷键完成，不能只依赖隐藏图标。
- 菜单文案全部走 `labels`（中英同步），不自带图标；只在文案会被 `label` 覆盖、且它不会自己画图标时才用系统 `role`。

### 导出

- 导出统一放在 File 菜单里。
- 图片导出把整篇画成一张连续长图；比单张位图能承受的长度更长时，续为编号阅读页，而不是报错：**任何长度的文档都要导得出来**。
- 导出分辨率就是设备像素比，不额外放大。
- **导出的是一篇文档，不是编辑器此刻的样子**：光标、选区、因「正在编辑」而显形的源码、属性区都不上纸。

## 3. 已知的坑

试过并确认不行的方向，以及会静默出错的实现约束。别再试一遍。

- **让标签条浮在标题栏之上**：同一条顶行的分裂，症状会散到线、色、按钮三处。
- **用系统 `role` 生成带图标的菜单项**：它会自己画图标、自己取英文名。
- **悬停才显示自定义滚动条滑块**：Chromium 不会因 hover 重绘。
- **在标题栏放一个会「出现 / 消失」的加号**：标签栏出现时它会突然不见。
- **交给系统的颜色必须是纯 `#rrggbb`**：系统不认 CSS Color 4，而渲染层拿到的计算值长成那样，整次设置会被拒，按钮停在建窗时那套颜色上。回报失败不许静默失败。
- **主题派生变量必须定义在主题作用域里**：定义在根作用域会被浅色默认值算死，之后不跟主题走。
- **跟着一个基准走的数值必须从基准算出来**：同一个基准（比如顶行行高）在渲染层、主进程和预载 HTML 里各有一份，改的时候必须一起动；写死的下游尺寸会在基准变了之后留在旧刻度上。
- **删 CSS 规则必须连规则体一起删**，禁止把选择器并进下一条规则（类型检查查不出来）。
- **hover 才出现的控件**：触发点必须自己可接收指针事件，命中区要覆盖从触发点到控件的整条路径，并且不要同时用禁用态当显隐门。
- **用调试器协议一次抓完整篇再切页**：命令可能中途丢掉目标，而且返回的 Promise 少一个 `await`，就会被 `finally` 里的销毁抢先，整条导出直接失败。
- **滚动切片再拼成长图**：预设页高不等于可截图高度，每个接缝会丢掉一条。
- **编辑器装饰的三种静默失效**：装饰不许跨行替换（会抛错、整层装饰中断）；不许在插件 `update` 里派发事务（插件会直接崩掉，之后不再更新）；装饰依赖的语法树是**按视口惰性解析**的，滚到没解析过的区域会显示原始源码。机制与写法见 [docs/editor-architecture.md](docs/editor-architecture.md)。

## 4. 动手与提交

- **把「感觉不对」翻译成可量的差异**：每一次「别扭」都能落到具体差异上。先在真实窗口里量（computed style + 截图取色），再动手；一次只动一个变量，改完再量一遍确认方向。
- 改完 UI 要在**真窗口真机**里点一遍，深色和浅色各看一遍；浏览器复现页只能验证 CSS 机制，验不了 Electron 的运行时行为。
- 新增控件前先找有没有同职责的控件、样式或交互；不为单个控件复制一套 tooltip、按钮或图标样式。
- 编辑器逻辑、IPC 逻辑与视觉样式分离；共享视觉样式放在主题目录里；动态创建图标用固定模板，不把用户输入拼进 SVG 或 HTML。
- 提交前过一遍：必要性（没有重复入口）→ 图标语言与同类一致 → 颜色来自主题、深浅都看过 → 没有新增常驻控件 → 菜单、快捷键与多标签行为没被破坏 → 真机点过、类型检查与构建通过、`git diff --check` 干净 → 动过导出、编辑器核心或滚动渲染，就跑对应的 `verify:*`。
